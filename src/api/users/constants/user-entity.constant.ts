/**
 * Relationships that are used for substitution in JOIN queries
 */
export const USER_DETAILS_RELATIONS: string[] = ['role'];
export const USER_DETAILS_COLUMNS = [
  'id',
  'name',
  'email',
  'status',
  'role',
  'createdAt',
  'updatedAt',
] as any;
